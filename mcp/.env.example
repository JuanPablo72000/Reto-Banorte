# --- DeepSeek primario (ia/ia_client.py, PlannerIA) ---
DEEPSEEK_API_KEY=tu_api_key_de_deepseek
DEEPSEEK_MODELS=deepseek-chat
DEEPSEEK_BASE_URL=https://api.deepseek.com
IA_MAX_RETRIES=2
IA_MAX_COMPLETION_TOKENS=8192

# --- Gemini secundario (ia/ia_client.py, fallback) ---
# Opcional: si se deja vacío, no se usa (queda solo DeepSeek). Se prueba
# SOLO después de agotar los modelos de DeepSeek, para no depender de
# un único proveedor cuando DeepSeek da 429.
GEMINI_API_KEY=tu_api_key_de_gemini
GEMINI_MODELS=gemini-2.5-flash

# --- Integración con la API real de Pablo (integration/api_client.py) ---
# Docker (docker-compose.yml): la API expone http://localhost:8000.
# El puerto 5178 solo aplica si corres el backend .NET local sin docker
# (Properties/launchSettings.json).
BANORTE_API_BASE_URL=http://localhost:8000
BANORTE_API_TIMEOUT=10
# Usuario semilla para el login automático (ver DbSeeder.cs). Solo sirve
# para el usuario demo; a futuro el token vendrá de la sesión del usuario.
BANORTE_API_EMAIL=demo@banorte.mx
BANORTE_API_PASSWORD=Demo123!
# Modo de las tools: auto (API real con fallback a mock) | api (solo real)
# | mock (solo mocks, comportamiento anterior a la integración).
BANORTE_API_MODE=auto

# --- Catálogo del contrato A2UI (ia/contract_catalog.py) ---
# Opcional: solo si el YAML no está en la ruta por defecto
# (contracts/a2ui/a2ui-mcp-contract.yaml, relativo a la raíz del repo).
# A2UI_CONTRACT_PATH=contracts/a2ui/a2ui-mcp-contract.yaml
